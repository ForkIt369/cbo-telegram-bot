{
  "name": "cbo-telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot interface for CBO agent",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/webhook-server.js",
    "start:single": "node src/index.js",
    "start:combined": "node src/combined-server.js",
    "dev": "nodemon src/webhook-server.js",
    "build": "cd mini-app && npm ci --production=false && npm run build",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.59.0",
    "@modelcontextprotocol/sdk": "^1.17.1",
    "axios": "^1.11.0",
    "dotenv": "^16.3.1",
    "eventsource": "^2.0.2",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "ngrok": "^5.0.0-beta.2",
    "telegraf": "^4.15.3",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
